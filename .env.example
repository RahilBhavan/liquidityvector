# Frontend Configuration
# ----------------------

# The URL of the backend API for server-side proxying.
# This is used by next.config.ts to rewrite requests from /api/backend/* to the actual backend.
# Development: http://localhost:8000
# Production: https://api.yourdomain.com
# Note: Do not include a trailing slash.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# The Base URL used by the frontend (apiService.ts) to make API calls.
# If using the Next.js proxy (recommended to avoid CORS), set this to: /api/backend
# If calling backend directly, set to the backend URL: http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=/api/backend

# API Key for Google Gemini (AI Service)
# Get one here: https://aistudio.google.com/app/apikey
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here

# WalletConnect Project ID for RainbowKit
# Get one here: https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Backend Configuration (for api/main.py)
# ---------------------------------------

# Comma-separated list of allowed origins for CORS
# In production, this should include your frontend domain.
# Example: https://myapp.com,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
